---
title: LLM Knowledge Base System
date: 2026-04-05
status: ready-for-implementation
goal: Migrate James's learning content into an Obsidian-native, concept-oriented knowledge base with automated ingestion and wiki compilation.
constraints: Python 3 (no external deps beyond stdlib + anthropic SDK for compiler), Obsidian vault at kb/, existing content in learning/, Claude Code CLI for LLM calls
---

# LLM Knowledge Base System — Implementation Spec

## Overview
A two-tier knowledge base (raw sources + compiled wiki) organized by domain (hard skills / soft skills), backed by Obsidian for graph navigation. Raw content is ingested from curated sources and migrated from the existing `learning/` directory. A compiler powered by Claude synthesizes raw content into cross-cutting concept articles in the wiki layer. Cron-driven scrapers keep raw content fresh. The whole system lives at `/home/james/src/leo/kb/`.

## Folder Structure
```
kb/                              # Obsidian vault root
├── .obsidian/
│   └── app.json                 # Minimal Obsidian config (wikilinks enabled)
├── hard/
│   ├── raw/
│   │   ├── _index.md            # Flat catalog of all hard-skills raw content
│   │   ├── do_not_index_sources/ # Archive-only: long-form original content (NOT compiled)
│   │   ├── aman-ai/
│   │   ├── chip-huyen/
│   │   ├── eugene-yan/
│   │   ├── lilian-weng/
│   │   ├── karpathy/
│   │   ├── cameron-wolfe/
│   │   ├── sebastian-raschka/
│   │   ├── nathan-lambert/
│   │   ├── simon-willison/
│   │   ├── jay-alammar/
│   │   └── louis-wang/
│   └── wiki/
│       └── _index.md            # Catalog of all hard-skills concept articles
└── soft/                        # leadership, managing up, exec comms, product, growth, coaching
    ├── raw/
    │   ├── _index.md            # Flat catalog of all soft-skills raw content
    │   ├── do_not_index_sources/
    │   │   ├── lennys-podcast/  # Full transcripts (272 files)
    │   │   └── ethan-evans/     # VOD .txt files
    │   ├── wes-kao/
    │   ├── lennys-podcast/      # Distilled theme extractions, NOT full transcripts
    │   ├── ethan-evans/         # Articles (.md only)
    │   └── jefferson-fisher/
    └── wiki/
        └── _index.md            # Catalog of all soft-skills concept articles
```

## Rules
- Everything in `raw/` compiles to wiki EXCEPT `raw/do_not_index_sources/`
- `raw/do_not_index_sources/` is archive — original long-form material for reference only
- Long-form content gets distilled before entering `raw/{source-slug}/`
- Short-form content goes directly to `raw/{source-slug}/`
- Compiler only reads from `raw/` (excluding do_not_index_sources/) to build wiki

## Data Model

### Raw Article (existing format, preserved as-is during migration)
```markdown
# {Title}

**Source:** {URL}
**Ingested:** {YYYY-MM-DD}
**Tags:** {comma-separated tags}

---

{content}
```

### Wiki Article (generated by compiler)
```markdown
---
concept: {Concept Name}
tags: [{tag1}, {tag2}, ...]
sources: [{relative path to raw source 1}, {relative path to raw source 2}, ...]
last_compiled: {YYYY-MM-DD}
related: [{related-concept-1}, {related-concept-2}, ...]
---

# {Concept Name}

{Synthesized content — definitions, key ideas, practical applications, nuances.
 Written as a standalone reference article. Uses [[wikilinks]] for cross-references.}

## Sources
- [[hard/raw/aman-ai/two-tower-models.md]]
- [[hard/raw/eugene-yan/retrieval.md]]
```

### Raw Index (`_index.md`) format
```markdown
# Hard Skills — Raw Content Index

> Auto-generated. {N} articles across {M} sources. Last updated: {YYYY-MM-DD}

| Title | Source | Tags | Path | Wiki Concepts |
|-------|--------|------|------|---------------|
| Two-Tower Models | aman-ai | retrieval, embeddings | [[hard/raw/aman-ai/two-tower-models.md]] | [[hard/wiki/two-tower-models.md|Two-Tower Models]] |
```

### Wiki Index (`wiki/_index.md`) format
```markdown
# Hard Skills — Wiki Index

> Auto-generated. {N} concept articles. Last updated: {YYYY-MM-DD}

| Concept | Description | Sources | Related |
|---------|-------------|---------|---------|
| [[hard/wiki/two-tower-models.md|Two-Tower Models]] | Dual-encoder architecture for retrieval... | 3 | [[hard/wiki/candidate-generation.md|Candidate Generation]] |
```

## Tasks

### T-1: Create KB directory structure and Obsidian config
Create the `kb/` vault with all subdirectories and minimal Obsidian configuration.

**Acceptance criteria:**
- [ ] `kb/` directory exists with all subdirectories matching the folder structure
- [ ] `.obsidian/app.json` exists with wikilinks enabled
- [ ] `.gitignore` updated to exclude Obsidian workspace/cache files but include `app.json`
- [ ] All source-slug directories exist
- [ ] Placeholder `_index.md` files exist in all 4 index locations

### T-2: Migration script
One-time script that copies all existing content from `learning/` to `kb/`. Does NOT delete originals.

**Migration mapping:**
| Source | Destination |
|--------|-------------|
| `learning/articles/{hard-slug}/*.md` | `kb/hard/raw/{slug}/` |
| `learning/articles/wes-kao/*.md` | `kb/soft/raw/wes-kao/` |
| `learning/articles/jefferson-fisher/*.md` | `kb/soft/raw/jefferson-fisher/` |
| `learning/articles/ethan-evans/*.md` | `kb/soft/raw/ethan-evans/` |
| `learning/articles/ethan-evans/*.txt` | `kb/soft/raw/do_not_index_sources/ethan-evans/` |
| `learning/articles/lennys-podcast/*.md` | `kb/soft/raw/do_not_index_sources/lennys-podcast/` |
| `learning/themes/{theme}/*.md` (flattened) | `kb/soft/raw/lennys-podcast/{theme}--{episode}.md` |

**Acceptance criteria:**
- [ ] `--dry-run` prints what would be copied without copying
- [ ] `--verify` checks all source files have a corresponding target
- [ ] Manifests copied with updated paths

### T-3: Build raw index generator
Script that scans all raw content (excluding `do_not_index_sources/`) and generates `_index.md` files.

**Acceptance criteria:**
- [ ] Regenerates all 4 index files
- [ ] Each entry includes: title, source slug, tags, relative path as wikilink
- [ ] Completes in under 5 seconds for ~2,400 files

### T-4: Update ingest.py and extract_themes.py to target kb/ paths
Update both existing scripts to write to `kb/` instead of `learning/`.

**Acceptance criteria:**
- [ ] `ingest.py` routes to correct `kb/{hard|soft}/raw/{slug}/` based on source
- [ ] `extract_themes.py` reads from `kb/soft/raw/do_not_index_sources/lennys-podcast/`, writes to `kb/soft/raw/lennys-podcast/`
- [ ] Both scripts call index generator after writing new content

### T-5: Wiki compiler
Core intelligence. Scans raw content, identifies cross-cutting concepts, produces wiki articles.

**Three phases:**
1. **Scan** — LLM reads raw catalog, identifies concepts + source mappings → JSON
2. **Plan** — Generates `_plan.md` for human review before compilation
3. **Compile** — For each concept, reads source articles, synthesizes wiki article

**Acceptance criteria:**
- [ ] `compile_wiki.py scan --domain hard` returns valid concept JSON
- [ ] `compile_wiki.py plan --domain hard` generates reviewable plan
- [ ] `compile_wiki.py compile --domain hard --all` compiles all concepts
- [ ] `compile_wiki.py incremental --domain hard` detects new/modified raw files, recompiles affected concepts
- [ ] Generated articles follow the wiki article schema
- [ ] All cross-references use `[[wikilinks]]`
- [ ] Tracks state in `.compile_state.json`

### T-6: Aman.ai dedicated scraper (cron)
Standalone script for aman.ai scraping, suitable for cron.

**Acceptance criteria:**
- [ ] Fetches aman.ai/search.json, ingests new content into `kb/hard/raw/aman-ai/`
- [ ] Idempotent — no duplicates
- [ ] Updates raw index after ingestion

### T-7: Web scouring agent (cron)
RSS-based content scout across all tracked sources. V1 is RSS; future `--discover` mode uses Claude.

**Acceptance criteria:**
- [ ] Checks each source's RSS feed, fetches new content, deposits as raw articles
- [ ] `--source X` scouts single source
- [ ] `--status` shows last scout time per source
- [ ] Rate-limited, cron-friendly logging

### T-8: Update CLAUDE.md and learning_agenda.md
Update Leo's system prompt to reflect the new KB structure.

**Acceptance criteria:**
- [ ] CLAUDE.md folder structure updated with `kb/` vault
- [ ] Context loading guide includes KB entry
- [ ] `learning/` references updated

## Skill Surface

Eight KB skills, hybrid architecture (Python scripts for deterministic work, Claude Code skills for LLM judgment):

| Skill | Type | Description |
|-------|------|-------------|
| `/kb-status` | Skill (wraps scripts) | Dashboard: article counts, scout times, index age, wiki state |
| `/kb-ingest` | Skill (wraps `ingest.py`) | Ingest URLs, PDFs, notes → routes to hard/soft, rebuilds indexes |
| `/kb-scout` | Skill (wraps `scout.py`) | Trigger RSS scout run for all or single source |
| `/kb-lint` | Hybrid (script + skill) | `kb_lint.py` for rule checks + LLM gap analysis layer |
| `/kb-compile` | Skill (LLM) | 3-phase wiki compiler: scan → plan (human review) → compile |
| `/kb-merge` | Skill (LLM) | Consolidate duplicate wiki concepts, update backlinks, archive |
| `/kb-reflect` | Skill (LLM) | Cross-cutting synthesis, gap detection. Manual-only, Leo surfaces proactively |
| `kb_search.py` | Python script | TF-IDF keyword search, cached index. Called by skills internally |

### Design Decisions (2026-04-05)
- **Hybrid architecture**: Python for deterministic (search, lint rules, indexing), skills for judgment (compile, reflect, merge)
- **Human-in-the-loop compile**: Plan phase required before compilation — James reviews concept groupings before tokens are spent
- **Separate wiki domains**: `hard/wiki/` and `soft/wiki/` kept separate (can merge later; reflect reads across both)
- **TF-IDF only for search**: Semantic fallback deferred until local LLM infra is in place
- **Reflect is manual**: Leo proactively suggests but never auto-runs. Same pattern as `/consult-notebook`
- **Inspired by**: Louis Wang's `llm-knowledge-base` (louiswang524/llm-knowledge-base) — cherry-picked 5 capabilities, adapted to James's existing script-based pipeline

## Out of Scope
- Deleting `learning/` directory (James does manually after verifying)
- Claude-powered content discovery (`--discover` mode)
- Automatic cron installation
- Embedding-based semantic search (deferred to local LLM — see backlog)
- NotebookLM integration (existing notebooks stay separate)

## Learner model — the map of James (added 2026-09-07)

Ratified in the 2026-09-07 grill (`system/session-logs/2026-09-07.md`). The KB stays the map of the field; this layer is the map of James, so lessons and paper suggestions are calibrated to what he actually knows.

- **Consumers of the KB (James, 9/7):** (1) the `learn` skill and Codex work over the next 2–3 months; (2) a paper inbox he feeds by hand (`ingest_paper.py` → `kb/hard/raw/arxiv/`). Hard side only; the soft wiki stays uncompiled. Graphify is skipped (Phase 2 stays frozen; no rebuild).
- **Unit:** the concept — the 65 `kb/hard/wiki/` slugs plus `EXTRA_CONCEPTS` in `scripts/kb_knowledge_state.py` (7 the wiki predates: semantic-id-tokenization, recsys-scaling-laws, serving-efficiency-and-user-state-caching, agent-harnesses-and-self-evolution, pre-ranking-cross-stage-consistency, user-foundation-models-and-distillation, offline-online-calibration-and-candidate-logging). 72 total.
- **Dimensions:** `understanding` ∈ {1 very little exposure (default), 2 basic — assumed where his background applies (PhD statistical ML; 12 yrs recsys; 4–5 content quality/integrity; 5 content understanding), 3 proven depth (demonstrated in dialogue/practice or authored), 4 boundary pushing}; `relevance` ∈ {0 not obviously, 2 somewhat/potentially, 3 very} relative to the role and `work/projects/personalization_retrieval_org_2027_agenda.md`.
- **Inheritance:** raw articles inherit from concepts matched by tag (article `**Tags:**` ∩ concept tags/slug) or by a specific concept phrase in the title; understanding = min over matches, relevance = max. Generic keys (`recsys`, `llm`, `retrieval`, …) never match on their own.
- **Storage and rendering:** source of truth `kb/.kb/knowledge_state.json` (committed; every change appends a dated, attributed evidence entry). Rendered into wiki frontmatter (`understanding`, `relevance`, `knowledge_updated`), into U/R columns of both `_index.md` catalogs by `build_index.py`, and exported to `self/learning/knowledge_state.md` (learning queue = relevance 3 & understanding ≤ 2).
- **Writers:** `learn` (practice/dialogue evidence), `context-update` (work/authored evidence), `ingest_paper.py --evidence`. Mostly inferred, corrected by evidence; reading is never evidence; levels can go down. The 9/7 seed pass is preserved in `self/learning/knowledge_state_seed_2026-09-07.json`.
- **Portability:** stdlib Python only, no LLM calls inside scripts; skills have `.agents/skills/` entry points generated by `scripts/leo_setup.py --project`, so Codex and Claude Code write the same file.
- **Diagnostic quiz:** `self/learning/diagnostic_quiz_2026-09.md` — broad, adaptive, taken in practice mode when James chooses; results written back with `set --kind practice`.
