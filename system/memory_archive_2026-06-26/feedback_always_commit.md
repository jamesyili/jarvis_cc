---
name: Always commit and push
description: Never skip committing and pushing code changes, even on short or skipped-log sessions
type: feedback
---

Always commit and push code at the end of every session, regardless of whether the session log is skipped.

**Why:** James explicitly corrected Leo for skipping the commit when the session log was skipped. Code changes should never be left uncommitted.

**How to apply:** Treat commit+push as a separate step from session logging. Even if James says "skip" to the session log, still commit and push any changes made during the session.
