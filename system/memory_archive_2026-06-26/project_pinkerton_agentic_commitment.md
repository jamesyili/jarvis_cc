---
name: Pinkerton agentic trajectory committed + blog deadline
description: James committed April 2026 to Fork A (big bet on Phase 4 simulation harness) in Pinkerton's 6-phase agentic roadmap, with blog post #1 as the synthesis guardrail
type: project
---

On 2026-04-05, after reviewing 5 agentic recsys papers (Adobe survey, MAVR, MACRec, CoELA, RecAgent), James committed to:

1. **Fork A — go big.** Pinkerton follows a 6-phase trajectory from single-agent M1 debugger all the way through agentic augmentation. Phase 4 (offline simulation harness) is committed with a hard gate: M2 profiles must pass a held-out prediction test before the sandbox gets built.

2. **Sequencing resolved:** M1 single-agent → M2 single-agent profiling module → multi-agent refactor when M3 scale forces it. Do not refactor M1 for aesthetics.

3. **Hands-on commitment:** James stays architect + builder personally through at least Phases 1-2. Staffing conversation deliberately deferred — revisit only once the project is visibly successful (probably post-M2 with measurable UIC eval value).

4. **Karen guardrail:** Pinkerton trajectory runs *alongside* blog post #1, not instead of it. Blog post #1 (pretrain-finetune in recsys) has a hard deadline of 2026-04-11 (next Saturday). Blog post stays P0. If the blog post slips, that is the signal that Pinkerton is eating synthesis time.

**Why:** Andrew invited James to co-own Detect + Diagnose in Reflex. Committing only to diagnosis (Fork B) forecloses on the Design / Verify conversation before it happens. Simulation harness is the piece that maps Pinkerton onto Reflex's full 7-stage pipeline.

**How to apply:**
- Treat `work+self/projects/pinkerton-agentic-vision.md` as a living strategy doc, not a committed roadmap. It is input to the Andrew / Reflex conversation.
- If James starts drifting into Phase 2+ work before M1 ships OR before blog post #1 is out, name the drift explicitly — that is exactly Karen's "hears feedback, builds infrastructure for the pivot instead of doing the pivot" pattern.
- M1 spec at `work+self/projects/pinkerton-m1-spec.md` is what actually ships in Q2. Phase 1+ stays a plan until M1 is demoable to Jeff.
- Blog post deadline 2026-04-11 is a tripwire. If not drafted by then, escalate.
